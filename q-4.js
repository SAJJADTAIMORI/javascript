// Prompt the user for their name
const userName = prompt("Please enter your name:");

// Define my name
const myName = "ali";

// Check if the user's name matches mine
if (userName === myName) {
    console.log("Hello, " + userName + "! It's nice to meet someone with the same name as me.");
}
